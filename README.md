# Tank-Level Control

The attached code implements a tank-level control algorithm for the CE105 coupled-tanks system, using National Instruments (NI) data acquisition (DAQ) hardware with multiple input/output channels to read sensors and drive motors.
